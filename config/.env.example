# ========================================

# ---- OPZIONI INTEGRATE CON LE API (OPZIONALI) ----
# Abilita il pre-check via API per verificare se il giocatore ha già votato oggi
# Se impostato a true, lo script chiamerà le API di Minecraft-Italia prima di cliccare
# per evitare click/azioni inutili. Usa con cautela (rate limits possibili).
# Esempio: USE_API_PRECHECK=true
USE_API_PRECHECK=false

# Endpoint base API (modificabile se necessario)
# API_BASE=https://minecraft-italia.net/lista/api

# Retry/backoff settings
# Quante volte riprovare le chiamate API prima di fare fallback
API_RETRIES=3
# Base delay (ms) per exponential backoff
API_BACKOFF_BASE_MS=500
# Timeout per chiamata API (ms)
API_TIMEOUT_MS=7000
# Min interval between API calls (ms) to reduce rate
API_MIN_INTERVAL_MS=200
# Cache TTL for server info (seconds)
API_CACHE_TTL_SEC=60
# FILE DI CONFIGURAZIONE - MINECRAFT-ITALIA AUTO-VOTE
# ========================================
# Questo è un file di esempio per la configurazione del bot
# 
# ISTRUZIONI:
# 1. Copia questo file e rinominalo in ".env" (senza .example)
# 2. Inserisci le tue credenziali reali
# 3. NON condividere mai il file .env con nessuno
# 4. NON committare il file .env su Git (è già in .gitignore)
#
# ========================================

# ================================================
# CONFIGURAZIONE MINECRAFT-ITALIA AUTO-VOTE BOT
# ================================================

# ---- CREDENZIALI DI ACCESSO (OBBLIGATORI) ----
# Email del tuo account minecraft-italia.net
EMAIL=tuo@email.com

# Password del tuo account minecraft-italia.net
PASSWORD=tuaPassword123

# URL del server da votare (pagina completa)
# Esempio: https://minecraft-italia.net/server/nomeserver/vota
SERVER_URL=https://minecraft-italia.net/server/nomeserver/vota

# ---- IMPOSTAZIONI VISUALIZZAZIONE ----
# Modalità headless (true = nascosto, false = visibile)
# Se lasci vuoto, il bot chiederà all'avvio
HEADLESS=true

# ---- PERSONALIZZAZIONE NOMI (OPZIONALI / AUTOMATICO) ----
# I nomi possono essere lasciati vuoti: lo script tenterà di rilevare
# il nickname del giocatore dalla sessione autenticata e il nome del
# server tramite le API o dall'URL. Se preferisci, puoi comunque
# impostare manualmente un valore qui per scopi di log.
# Esempio (opzionale):
# PLAYER_NAME=TuoNickname
# SERVER_NAME=NomeDelTuoServer

# ================================================
# NOTE:
# - Questo file è nella cartella config/ per sicurezza
# - Non condividere mai questo file con le tue credenziali
# - Usa HEADLESS=false per vedere il browser durante il debug
# - Il bot cerca automaticamente i file in config/ se non li trova nella directory principale
# ================================================

# ========================================
# NOTE SULLA SICUREZZA
# ========================================
# 
# IMPORTANTE:
# - Questo file contiene dati sensibili (password)
# - Non condividerlo MAI con nessuno
# - Non caricarlo su repository pubblici
# - Il file cookies.json generato contiene anche dati di sessione
# 
# ========================================